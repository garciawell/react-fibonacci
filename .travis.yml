sudo: required

services: -docker

before_install:
  - docker build -t garcia/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true garcia/react-test npm test

after_success:
  - docker build -t garcia/multi-client ./client
  - docker build -t garcia/multi-nginx ./nginx
  - docker build -t garcia/multi-server ./server
  - docker build -t garcia/multi-worker ./worker

  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push garcia/multi-client
  - docker push garcia/multi-nginx
  - docker push garcia/multi-server
  - docker push garcia/multi-worker
